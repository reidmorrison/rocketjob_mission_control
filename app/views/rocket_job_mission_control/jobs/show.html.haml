= react_component('Queue', jobs: ActiveModel::ArraySerializer.new(@jobs, each_serializer: JobSerializer).to_json)

-# #queue
-#   .col-md-4.job-list
-#     .filter.pull-right.btn-group.btn-group-justified{ data: { toggle: 'buttons' } }
-#       - %w(queued running failed completed).each do |state|
-#         %label.btn.btn-default.state-toggle{ class: ('active' if @states.include?(state.to_s) || @states.empty?) }
-#           = check_box_tag(state, state, (@states.include?(state.to_s) || @states.empty?))
-#           %i.fa{class: job_state_icon(state.to_sym)}
-#           = state.capitalize
-#
-#     .clearfix
-#     .list
-#       - if @jobs.empty?
-#         .card.inner
-#           .title
-#             .lead.text-center No Jobs Found.
-#
-#       - else
-#         - @jobs.each do |job|
-#           = react_component('jobList',
-#               {title: job_title(job),
-#               description: job.description.try(:truncate, 50),
-#               state: job.state,
-#               duration: job_duration(job)},
-#               {tag: 'a', class: 'card', href: job_path(job)},
-#             )
-#
-#   .col-md-8.job-status
-#     #job_details
-#       = react_component('JobDetails', JobSerializer.new(@job).as_json, {})
